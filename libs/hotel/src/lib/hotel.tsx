import React from 'react';
import { View, Text } from 'react-native';

/* eslint-disable-next-line */
export interface HotelProps {
}

export function Hotel(props: HotelProps) {
  return (
    <View>
      <Text>Welcome to hotel!</Text>
    </View>
  );
}

export default Hotel;
